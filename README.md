# Generate HTML from Frames

This is a code bundle for Generate HTML from Frames. The original project is available at https://www.figma.com/design/HWRJqipJ6BsNfRR893SPUQ/Generate-HTML-from-Frames.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
